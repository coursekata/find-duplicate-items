pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx}"
      run: npm exec biome -- check --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    typecheck:
      run: npx tsc --noEmit
